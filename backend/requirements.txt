# CollisionGuard AI — Phase 1 backend dependencies
# Pin minor versions; patch updates are allowed via pip install --upgrade.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# ── Validation & settings ─────────────────────────────────────────────────────
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
httpx>=0.27,<1.0          # TestClient transport for FastAPI

# ── Propagation (Phase 2) ─────────────────────────────────────────────────────
sgp4>=2.21,<3.0
skyfield>=1.49,<2.0
numpy>=1.26,<3.0

# ── Granite / watsonx.ai (Phase 6) ───────────────────────────────────────────
# Range >=1.1,<2.0 is used because ibm-watsonx-ai 1.x has a stable public API
# within the minor series. The verified installed version is 1.6.3.
# To pin exactly: ibm-watsonx-ai==1.6.3
ibm-watsonx-ai>=1.1,<2.0
