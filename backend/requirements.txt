# CollisionGuard AI — Phase 1 backend dependencies
# Pin minor versions; patch updates are allowed via pip install --upgrade.

# ── Web framework ─────────────────────────────────────────────────────────────
fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0

# ── Validation & settings ─────────────────────────────────────────────────────
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=8.0,<9.0
httpx>=0.27,<1.0          # TestClient transport for FastAPI

# ─────────────────────────────────────────────────────────────────────────────
# DEFERRED — do not install until the phase noted
# sgp4>=2.21          Phase 2  TLE propagation
# skyfield>=1.49      Phase 2  ephemeris / frame transforms
# numpy>=1.26         Phase 2  orbital math arrays
# ibm-watsonx-ai      Phase 3  Granite LLM via watsonx.ai
# ─────────────────────────────────────────────────────────────────────────────
